// Main component export
export { default } from "./CyberpunkTerminal";

// Types export for external usage
export type { CyberpunkTerminalProps, Command } from "./types";

// Utility exports that might be useful elsewhere
export { detectMacOS, getCurrentPath, formatTime } from "./utils";
